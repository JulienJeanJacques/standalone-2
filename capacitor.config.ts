import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '5_astrodidact-V5-standalone-2',
  webDir: 'www'
};

export default config;
